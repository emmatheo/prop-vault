# Render Blueprint: deploys the Prop Vault backend + frontend as one service.
# In Render: New + -> Blueprint -> pick this repo -> it reads this file.
# Set KEYPAIR_JSON when prompted (paste the contents of backend/keypair.json).
services:
  - type: web
    name: prop-vault
    runtime: node
    rootDir: backend
    plan: starter # has a persistent disk; 'free' works but loses markets.json on restarts and sleeps when idle
    # Render sets NODE_ENV=production, which makes `npm install` skip
    # devDependencies. tsx (the runtime) now lives in dependencies so it is
    # always installed; --include=dev is belt-and-suspenders for local parity.
    buildCommand: npm install --include=dev
    startCommand: npx tsx src/server.ts
    healthCheckPath: /health
    disk:
      name: prop-vault-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /var/data
      - key: NETWORK
        value: devnet
      - key: KEYPAIR_JSON
        sync: false # paste the [12,34,...] array from backend/keypair.json in the dashboard

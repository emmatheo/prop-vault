[toolchain]
anchor_version = "0.30.1"

[programs.devnet]
prop_vault = "PropVau1t1111111111111111111111111111111111"  # replace after `anchor keys sync`

[provider]
cluster = "devnet"
wallet = "backend/keypair.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
